@model ExamInvigilationManagement.Common.PagedResult<
    ExamInvigilationManagement.Application.DTOs.Admin.User.UserDto>

@{
    int stt = (Model.Page - 1) * Model.PageSize + 1;
}

<table class="table modern-table wide-table">
    <thead>
        <tr>
            <th class="col-id">STT</th>
            <th class="col-code">Tên đăng nhập</th>
            <th class="col-person">Họ tên</th>
            <th class="col-role">Vai trò</th>
            <th class="col-faculty">Khoa</th>
            <th class="col-status">Trạng thái</th>
            <th class="col-action">Thao tác</th>
        </tr>
    </thead>

    <tbody>
        @foreach (var item in Model.Items)
        {
            <tr>
                <td class="col-id">@stt</td>
                <td class="col-code">@item.UserName</td>
                <td class="col-person">@item.FullName</td>
                <td class="col-role">@item.RoleName</td>
                <td class="col-faculty">@item.FacultyName</td>
                <td class="col-status">
                    @if (item.IsActive)
                    {
                        <span class="badge bg-success">Đang dùng</span>
                    }
                    else
                    {
                            <span class="badge bg-danger">Đã vô hiệu hóa</span>
                    }
                </td>
                <td class="col-action">
                    <div class="action-group">
                        <a asp-area="Admin" asp-action="Edit" asp-route-id="@item.Id" class="btn-icon edit">
                            <i class="bi bi-pencil"></i>
                        </a>

                        @if (item.IsActive)
                        {
                            <a href="#"
                               class="btn-icon delete btn-delete"
                               data-id="@item.Id"
                               data-name="@item.UserName"
                               data-modal-title="Xác nhận vô hiệu hóa"
                               data-modal-message="Bạn có chắc muốn vô hiệu hóa tài khoản"
                               data-modal-confirm="Vô hiệu hóa">
                                <i class="bi bi-slash-circle"></i>
                            </a>
                        }
                    </div>
                </td>
            </tr>
            stt++;
        }
    </tbody>
</table>

@await Html.PartialAsync("_Pagination", Model)
